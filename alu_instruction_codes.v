parameter    HOLD  = 0;
parameter    CLEAR = HOLD + 1;
parameter    ADD   = CLEAR + 1;
parameter    SUB   = ADD + 1;
parameter    AND   = SUB + 1;
parameter    NEG   = AND + 1;
parameter    NOT   = NEG + 1;
parameter    XOR   = NOT + 1;